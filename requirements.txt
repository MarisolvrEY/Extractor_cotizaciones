# HTTP — para las REST APIs de Azure AI Foundry

# Archivos comprimidos
rarfile>=4.1.0        # para .rar (requiere también unrar instalado en el sistema)

# Emails (.msg de Outlook) — opcional, solo si tienes archivos .msg
extract-msg>=0.48.0
requests>=2.32.0

# Data
pandas>=2.2.0
openpyxl>=3.1.0

# Environment
python-dotenv>=1.0.0

# Utilidades
tqdm>=4.66.0
rich>=13.7.0
tenacity>=8.3.0

# Testing
pytest>=8.0.0
pytest-mock>=3.12.0

# Typing
typing-extensions>=4.11.0

# Conversión email → PDF (step0_preparar.py)
weasyprint>=60.0      # mejor calidad HTML→PDF (recomendado)
# xhtml2pdf>=0.2.16  # alternativa a weasyprint
# fpdf2>=2.7.9       # fallback solo-texto

# Metadatos (step3_metadatos.py / meta_1.py)
Pillow>=10.0.0
piexif>=1.1.3
pypdf>=4.0.0
python-docx>=1.1.0
python-pptx>=0.6.23
mutagen>=1.47.0
xlrd>=2.0.1
ebooklib>=0.18
